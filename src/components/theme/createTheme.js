import { createTheme } from "@mui/material/styles";

// Configurações de tipografia em todo o sistema
const theme = createTheme({
  typography: {
    fontFamily: "Open Sans, Roboto, Helvetica, Arial, sans-serif",
  },
});

export default theme;
